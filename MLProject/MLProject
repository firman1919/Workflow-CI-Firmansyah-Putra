name: coffeeshop-kmeans

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      dataset:
        type: string
        default: coffeshop_preprocessing.csv
    command: "python modelling.py {dataset}"
